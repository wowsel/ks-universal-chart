ingresses:
  app1:
    tls:
      - secretName: example-tls
        hosts:
          - example.com
    hosts:
      - host: example.com
        paths:
          - path: /
            pathType: Prefix
            port: 80