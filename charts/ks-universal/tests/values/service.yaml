deployments:
  app1:
    containers:
      main:
        image: nginx
        imageTag: 1.21
services:
  app1:
    type: ClusterIP
    ports:
      - name: http
        port: 80
        targetPort: 80
        protocol: TCP