# tests/values/auto-service.yaml
deployments:
  app1:
    autoCreateService: true
    containers:
      main:
        image: nginx
        imageTag: 1.21
        ports:
          http:
            containerPort: 80